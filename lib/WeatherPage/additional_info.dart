import 'package:flutter/material.dart';

class AdditionalInfo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      height: 50,
      color: Colors.cyan,
      margin: EdgeInsets.all(10),
      child: Text('Доп инфа о погоде WIP'),
    );
  }
}
